import React from 'react';

const HeaderView = () => (
    <nav>
        <div class="nav-wrapper">
            <span class="brand-logo center">
                Todo Application
            </span>
        </div>
    </nav>

);

export default HeaderView;